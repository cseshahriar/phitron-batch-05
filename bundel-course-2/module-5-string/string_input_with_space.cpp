#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    cin.ignore(); // ignore entire / new line character
    // getchar(); // ignore entire / new line character
    cout << n << endl;

    // string with spaces
    string s;
    getline(cin, s); // one line, take enter as string
    cout << s << endl;

    return 0;
}