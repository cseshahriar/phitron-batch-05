#include <stdio.h>
int main() {
    long long int A, B, multiplication = 0;
    scanf("%lld", &A);  
    scanf("%lld", &B);
    multiplication = A * B;
    printf("%lld", multiplication);
   return 0;
}