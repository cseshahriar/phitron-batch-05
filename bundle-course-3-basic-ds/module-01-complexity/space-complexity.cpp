#include <bits/stdc++.h>
using namespace std;
// input space + auxiliary space
// max size 10^6 is possible
int main() {
    int n;
    cin >> n;
    int arr[n]; // input space
    int brr[5]; // auxiliary space

    int a, b;
    cin >> a >> b;
    int s = a + b; // a and b is input space // o (1)
    // s is auxiliary space O(1)
    return 0;

}